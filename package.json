{
  "name": "mcp-server-gsc-pro",
  "version": "1.2.3",
  "description": "Enhanced MCP server for Google Search Console — fresh data, hourly analytics, search type filtering, content decay detection, keyword cannibalization, and more",
  "author": "Ricardo <ricardo@nth.one>",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-server-gsc-pro": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "google-search-console",
    "gsc",
    "seo",
    "search-analytics"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ricardo-nth/mcp-server-gsc"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run --exclude tests/mcp-smoke.test.ts",
    "test:mcp-smoke": "vitest run tests/mcp-smoke.test.ts",
    "test:watch": "vitest --exclude tests/mcp-smoke.test.ts",
    "lint": "tsc --noEmit -p tsconfig.check.json",
    "docs:sync": "tsx scripts/docs-tools.ts sync",
    "docs:check": "tsx scripts/docs-tools.ts check",
    "release:check": "tsx scripts/release-check.ts",
    "package:check": "npm pack --dry-run",
    "prepublishOnly": "pnpm lint && pnpm test && pnpm build && pnpm test:mcp-smoke && pnpm docs:check && pnpm release:check"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "google-auth-library": "^9.15.0",
    "googleapis": "^146.0.0",
    "zod": "^3.25.0",
    "zod-to-json-schema": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
